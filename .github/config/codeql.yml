name: "Custom CodeQL Config"

# Query suites
queries:
  - uses: security-extended
  - uses: security-and-quality

# Paths to exclude
paths-ignore:
  - "spec/**"
  - "docs/**"
  - "helm_deploy/laa-estimate-eligibility/values/*.yaml"

# Paths to include (overrides ignore)
paths:
  - "app/**"