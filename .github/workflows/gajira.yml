name: Raise Dependabot Jira Issue

on:
  pull_request:
    types: [opened]

jobs:
  gajira:
    name: Raise Jira Issue
    runs-on: ubuntu-latest
    if: ${{ contains(github.event.pull_request.labels.*.name, 'dependencies') }}

    steps:
    - name: Login
      uses: atlassian/gajira-login@ca13f8850ea309cf44a6e4e0c49d9aa48ac3ca4c # v3
      env:
        JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
        JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
        JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}

    - name: Create Jira ticket
      id: create
      uses: atlassian/gajira-create@1ff0b6bd115a780592b47bfbb63fc4629132e6ec # v3
      with:
          project: EL
          issuetype: Task
          summary: |
            CCQ Dependabot: ${{ github.event.pull_request.title }}, GitHub PR#${{github.event.number}}  
          description: |
            Please view the PR for further information.
            PR link: https://github.com/${{ github.repository }}/pull/${{ github.event.pull_request.number }}
            
            In addition, please resolve other issues, if any. 
