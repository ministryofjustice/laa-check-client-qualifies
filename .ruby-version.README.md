The CFE API pipeline currently executes a set of end-to-end tests under the `end2end_tests` job in https://github.com/ministryofjustice/cfe-civil/blob/main/.circleci/config.yml which may need updating in line with any changes to CCQ's `.ruby-version` to avoid breaking that pipeline. 

Check with that team when updating the Ruby version.