apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-estimate-eligibility.fullname" . }}
  labels:
    {{- include "laa-estimate-eligibility.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-estimate-eligibility.selectorLabels" . | nindent 4 }}
