apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-estimate-eligibility.fullname" . }}
  labels:
{{ include "laa-estimate-eligibility.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "laa-estimate-eligibility.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
