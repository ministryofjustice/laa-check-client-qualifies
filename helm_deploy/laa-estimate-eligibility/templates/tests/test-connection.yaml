apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-estimate-eligibility.fullname" . }}-test-connection"
  labels:
{{ include "laa-estimate-eligibility.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "laa-estimate-eligibility.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
